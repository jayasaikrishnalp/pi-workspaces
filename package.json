{
  "name": "pi-workspace-server",
  "version": "0.1.0",
  "description": "Web workspace wrapping the pi agent CLI for WK CloudOps SREs.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test:smoke": "node --import tsx --test tests/*.smoke.mjs",
    "test:unit": "node --import tsx --test tests/*.test.mjs",
    "test:integration": "node --import tsx --test tests/integration/*.smoke.mjs",
    "test": "node --import tsx --test tests/*.test.mjs tests/*.smoke.mjs",
    "test:all": "node --import tsx --test tests/*.test.mjs tests/*.smoke.mjs tests/integration/*.smoke.mjs"
  },
  "author": "WK CloudOps Hackathon",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "4.19.4",
    "typescript": "5.6.3"
  }
}
