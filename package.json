{
  "name": "pi-workspace-server",
  "version": "0.1.0",
  "description": "Web workspace wrapping the pi agent CLI for WK CloudOps SREs.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test:smoke": "PI_WORKSPACE_AUTH_DISABLED=1 node --import tsx --test --test-concurrency=1 tests/*.smoke.mjs",
    "test:unit": "PI_WORKSPACE_AUTH_DISABLED=1 node --import tsx --test --test-concurrency=1 tests/*.test.mjs",
    "test:integration": "PI_WORKSPACE_AUTH_DISABLED=1 node --import tsx --test --test-concurrency=1 tests/integration/*.smoke.mjs",
    "test": "PI_WORKSPACE_AUTH_DISABLED=1 node --import tsx --test --test-concurrency=1 tests/*.test.mjs tests/*.smoke.mjs",
    "test:all": "PI_WORKSPACE_AUTH_DISABLED=1 node --import tsx --test --test-concurrency=1 tests/*.test.mjs tests/*.smoke.mjs tests/integration/*.smoke.mjs",
    "smoke:mcp": "node --import tsx --test tests/integration/mcp-live.manual.mjs"
  },
  "author": "WK CloudOps Hackathon",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "22.10.5",
    "@types/sanitize-html": "^2.16.1",
    "tsx": "4.19.4",
    "typescript": "5.6.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "chokidar": "^4.0.3",
    "sanitize-html": "^2.17.3"
  }
}
