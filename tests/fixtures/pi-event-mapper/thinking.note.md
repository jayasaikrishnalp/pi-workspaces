# thinking

Extended-thinking blocks stream like text but on the `thinking.*` event family so the UI can render them collapsed. `messageId` is allocated on the surrounding `message_start` and reused for the thinking sub-events.
